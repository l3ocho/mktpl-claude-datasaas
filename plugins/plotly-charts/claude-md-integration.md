# plotly-charts CLAUDE.md Integration

## Integration Snippet

```markdown
## Plotly Charts (plotly-charts)

### Commands
- `/chart create <type>` — Scaffold a theme-aware Plotly chart
- `/chart export <format>` — Export chart configuration

### Notes
- plotly-charts reads theme colors from `.claude/design-contract.json` (dmc-design)
- Install dmc-design alongside plotly-charts for full theme integration
```
